#include <stdio.h>
Int main (){
    int age;
    Printf("Enter a age");
    scanf("%d",&age);

    if (age >=18){
        printf("\n The person are eligible to vote");
    }else{
        printf("\n The person are not eligible to vote");
    }
    return 0;
}