include<stdio.h>
include<conio.h>

int main(){
    int a,b;
    printf("Enter the first number:");
    scanf("%n",&a);
    printf("Enter the first number:");
    scanf("%n",&b); 
    
    int sum = a+b;
    prinf("Sum of the two numbers is: ",sum);

    return 0;
}